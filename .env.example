# Copy to .env and fill in. .env is gitignored; never commit real keys.
# Optional: raises Google Books rate limits for large runs. Without it the
# pipeline still works, but the class-level call-number fallback (which reads
# Google Books categories) degrades while anonymous quota is throttled.
# The CLI auto-loads .env on startup.
GOOGLE_BOOKS_API_KEY=
